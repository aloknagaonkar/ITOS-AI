# ITOS v8.4.0 Validation

- All Python source files passed Python bytecode compilation.
- The application retains the v8.3 engine modules and dependencies.
- The new navigation uses two Streamlit tabs while preserving existing widgets and styling.
- A live Upstox/Streamlit runtime test was not performed in this build environment because Streamlit is not installed and no broker session token is available.
