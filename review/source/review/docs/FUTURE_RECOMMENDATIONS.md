# Future Recommendations

- Consider consolidating the two historically named institutional-confidence implementations in a later, explicitly scoped migration.
- Consider a typed historical-series contract so timeline and strike-level gamma-wall inputs can migrate without changing their formulas.
- Remove legacy mapping adapters only after all external callers have migrated.
